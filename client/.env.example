# Leave empty for local dev (Vite proxies /api and /health to localhost:5000).
# In production, set this to your deployed backend's absolute URL, e.g.
# https://lexiclear-api.vercel.app
VITE_API_URL=
